import 'package:flutter/material.dart';

/// Job posting screen
class JobPostingScreen extends StatelessWidget {
  const JobPostingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tạo tin tuyển dụng')),
      body: const Center(child: Text('Job Posting Screen - To be implemented')),
    );
  }
}
